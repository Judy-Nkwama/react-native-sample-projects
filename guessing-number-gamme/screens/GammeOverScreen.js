import { View, Text, StyleSheet } from "react-native";

const GammeOverScreen = props => {
    return(
        <View>
            <Text>Gamme is over...</Text>
        </View>
    );
};

const styles = StyleSheet.create({

});

export default GammeOverScreen;